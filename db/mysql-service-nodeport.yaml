apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: db
spec:
  selector:
    app: mysql
  ports:
  - port: 3306
    targetPort: 3306
    nodePort: 30036  # 范围 30000-32767
  type: NodePort